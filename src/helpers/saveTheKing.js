function saveTheKing(attack) {
    /** 
     * FIND ALL PIECES ABLE TO SAVE THE KING THROUGH TAKING OR BLOCKING 
    */
    const boardMatrix = attack.boardMatrix

    if (Object.keys(attack).includes('k') || Object.keys(attack).includes('K')) {

    }

}

export default saveTheKing